import { Typography } from '@mui/material';
import Layout from '../components/layout/Layout';

export default function Home() {
	return (
		<div>
			<Layout>
				<Typography fontSize={30}>Hello word</Typography>
			</Layout>
		</div>
	);
}
